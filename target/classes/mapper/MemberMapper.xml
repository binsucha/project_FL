<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberMapper">

	<select id="selectAllMember" resultType="MemberDTO">
		select * from member
	</select>
	
	<!-- 회원 가입 데이터 중복 확인 -->
	<select id="checkId" parameterType="hashmap" resultType="MemberDTO">
		select * from member
		<choose>
			<when test="type=='id'"><!-- #{type}으로 하면 오류, key 값을 어떻게 인식하는 걸까? -->
				where id = #{data}
			</when>
			<when test="type=='memberName'">
				where member_name = #{data}
			</when>
			<when test="type=='email'">
				where email = #{data}
			</when>
			<otherwise>
				where phone = #{data}
			</otherwise>
		</choose>
	</select>
	
	<!-- 회원 가입 -->
	<insert id="joinMember" parameterType="map">
		insert into member (id, password, member_name, email, phone, member_role)
		values (#{id}, #{password}, #{member_name}, #{email}, #{phone}, 1)
	</insert>
</mapper>